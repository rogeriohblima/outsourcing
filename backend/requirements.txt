# ============================================================
# Dependencias do Backend — Sistema de Contrato de Impressoras
# ============================================================

# --- Framework Web ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- ORM e Banco de Dados ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
aiosqlite==0.20.0
psycopg2-binary==2.9.10

# --- Validacao de Dados ---
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Autenticacao ---
# IMPORTANTE: instale pycryptodome (nao pycrypto).
# Se aparecer "No module named 'Crypto'", execute:
#   pip uninstall pycrypto -y
#   pip install pycryptodome
python-jose==3.3.0
pycryptodome==3.21.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.19
ldap3==2.9.1

# --- SNMP ---
pysnmp==6.1.3
pyasn1==0.6.1

# --- Utilitarios ---
python-dotenv==1.0.1          # Carrega .env no seed.py e em dev local
python-dateutil==2.9.0
httpx==0.28.0

# --- Testes ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-env==1.1.5
anyio==4.7.0
